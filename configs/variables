
# VM_ variables used by Vagrantfile to configure VM build
VM_FORWARDPORTS="FALSE" # Default is TRUE
VM_USEMAKE="FALSE"     # Default is FALSE

export VM_FORWARDPORTS VM_USEMAKE


# Additional variables used by various bash provisioners

HOME_DIR="/home/vagrant"

# Shared comes from shell provisioner in VagrantFile
DOWNLOAD_DIR="$SHARED_DIR/downloads"

DJATOKA_HOME="/usr/local/djatoka"

FEDORA_VERSION=3.8.0
FEDORA_HOME="/usr/local/fedora"

FFMPEG_VERSION=1.1.4

FITS_HOME="/usr/local/fits"
FITS_VERSION=0.8.5

SOLR_HOME="/usr/local/solr"
SOLR_VERSION=4.2.0

DRUPAL_HOME="/var/www/drupal"

ISLANDORA_MAKEFILE="/vagrant/configs/islandora-HEAD.make"
DRUPAL_MAKEFILE="/vagrant/configs/drupal.make"
